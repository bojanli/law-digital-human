Based on the analysis of the provided design document and the current codebase, I will now proceed with **Sprint 4: Digital Human Multimodal (Frontend Bridge & Backend TTS)**.

### Current Status
*   **Sprint 1-3 Completed**: Knowledge base, RAG chat, and Case FSM are implemented.
*   **Frontend**: `avatarBridge.ts` and `AvatarContainer.vue` exist but need refinement to strictly match the protocol in Section 7.2 of the design doc.
*   **Backend**: `tts.py` exists and has a "mock" provider which generates 250ms silence. `chat.py` and `case.py` already call `tts_service.synthesize`.

### Implementation Plan (Sprint 4)

1.  **Backend TTS Enhancement**
    *   The current mock TTS returns silence. I will update `backend/app/services/tts.py` to support a more realistic mock (e.g., returning a valid WAV header with silence is already done, but I will ensure it's robust) or a simple local TTS if requested. For now, the existing mock is sufficient for "Sprint 4" *integration testing* if we don't have a real API key.
    *   **Action**: I will double-check `backend/app/core/config.py` to ensure `TTS_PROVIDER` defaults to `mock` so it works out of the box.

2.  **Frontend Unity Bridge Refinement**
    *   The design doc (Section 7.2) specifies:
        *   `Avatar.Play(audioUrl, subtitleText)`
        *   `Avatar.SetEmotion(emotionTag)`
        *   `Avatar.SetGesture(gestureTag)`
        *   `Avatar.Stop()`
        *   Callbacks: `OnAvatarReady()`, `OnPlayFinished()`
    *   **Action**: I will verify `frontend/src/services/avatarBridge.ts` against these exact signatures. The current implementation looks mostly correct but I will ensure the parameter names match exactly (`audioUrl` vs `url`, etc.) to avoid integration issues.
    *   **Action**: I will update `AvatarContainer.vue` to handle the Unity loader script. Since I cannot build the actual Unity project, I will create a **Mock Unity Loader** (`frontend/public/unity-mock.js`) that simulates the Unity instance. This allows the frontend to be fully tested (receiving "Ready" events, sending messages, and getting "PlayFinished" callbacks) without the heavy 3D engine.

3.  **Verification**
    *   I will create a verification script (or manual test steps) to confirm:
        *   API returns `audio_url`.
        *   Frontend receives `audio_url`.
        *   Frontend sends `Avatar.Play` to the (mock) Unity instance.
        *   (Mock) Unity instance sends `OnPlayFinished` back.

I will start by creating the **Mock Unity Loader** and ensuring the configuration is set for success.
